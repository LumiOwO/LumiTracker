## 1. Documentation Setup

- [x] 1.1 Create main project rules markdown file in `./agent` to document the code conventions.
- [x] 1.2 Document the requirement that all future agent changes/specs remain in `./agent` and temp files go in `./agent/temp`.
- [x] 1.3 Update any relevant repository readmes to point to this new agent rules documentation (if required).

## 2. Structural Adjustments

- [x] 2.1 Ensure the `./agent` and `./agent/temp` directories exist.
- [x] 2.2 Verify that OpenSpec CLI recognizes the correct working directory path for future changes.
